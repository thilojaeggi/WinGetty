{% extends "base.j2" %}
{% set title = "Setup" %}
{% block content %}
<h1 class="text-3xl font-medium text-gray-800 dark:text-gray-300">{{title}}</h1>



<p class="mt-4 inline-block dark:text-gray-200">Setting up is easy, just execute the following command in an elevated PowerShell Terminal to add WinGetty as a source.</p><br>
<p class="inline-block p-2 bg-white dark:bg-neutral-900 dark:text-gray-300 rounded-md mt-2">winget source add -n WinGetty -t "Microsoft.Rest" -a {{ url_for('ui.index', _external=True) }}</p>

{% endblock %}